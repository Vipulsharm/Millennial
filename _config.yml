baseurl: ""  # This should be an empty string for your custom domain
url: "https://tarasharma.in"  # This should be your custom domain
# Site Build Settings

# jekyll-paginate configuration
paginate:            5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           /:title
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           "Taralittlestar"
description:	       "Tara Sharma's personal site highlighting the best content from her life"
author:              "Tara"

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
